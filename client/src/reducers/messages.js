const initialState = {
    inputText: '',
    messages: [],
    lastSender: null,
    messageMenu: false,
    loginToggle: false,
};

const messages = (state = initialState, action = {}) => state;

export default messages;
